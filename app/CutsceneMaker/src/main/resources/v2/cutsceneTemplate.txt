function %s()
    local Cutscene = {
        RestoreGameSpeed = %b,
        HideBorderPins   = %b,
        TransperentBars  = %b,

%s
        Finished = function(_Data)
        end
    };
    return API.StartCutscene(Cutscene);
end