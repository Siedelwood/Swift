function %s()
    local Cutscene = {
        RestoreGameSpeed = %b,
        HideBorderPins   = %b,
        TransperentBars  = %b,

%s
        Finished = %s;
    };
    return API.StartCutscene(Cutscene);
end