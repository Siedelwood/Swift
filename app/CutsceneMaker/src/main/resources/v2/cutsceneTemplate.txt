function %s()
    local Cutscene = {
        RestoreGameSpeed = %b,
        HideBorderPins   = %b,
        TransperentBars  = %b,
        FastForward      = %b,

%s
        Starting = %s;
        Finished = %s;
    };
    return API.CutsceneStart(Cutscene);
end