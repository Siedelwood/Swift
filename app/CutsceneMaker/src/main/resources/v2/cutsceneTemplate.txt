function %s()
    local Cutscene = {
        RestoreGameSpeed = %b,
        HideBorderPins   = %b,
        TransperentBars  = %b,
        FastForward      = %b,

%s
        Finished = %s;
    };
    return API.CutsceneStart(Cutscene);
end