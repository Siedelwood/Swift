function %s()
    local Cutscene = {
        RestoreGameSpeed = %b,
        HideBorderPins   = %b,
        BigBars          = %b,
        BarOpacity       = %f,
        FastForward      = %b,
    };
    local AF = API.AddFlights(Cutscene);

%s
    Cutscene.Starting = %s;
    Cutscene.Finished = %s;

    return API.CutsceneStart(Cutscene);
end