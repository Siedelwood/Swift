function %s()
    local Cutscene = {
        RestoreGameSpeed = %b,
        HideBorderPins   = %b,
        BigBars          = %b,
        BarOpacity       = %f,
        FastForward      = %b,

%s
        Starting = %s;
        Finished = %s;
    };
    return API.CutsceneStart(Cutscene);
end